- hosts: vagrants
  tasks:
    - name: 末尾に文字を複数行追加
      lineinfile:
        dest: /tmp/test.txt
        line: "{{ item }}"
      with_items:
        - "aaa"
        - "bbb"


    - name: エラー実験
      shell: "sh /tmp/test.sh"

#  vars:
#    sample_vars: software_desing_2018
#    - shell: uptime
#      register: result
#
#    - debug:
#        msg: "{{ result.stdout }}"
#
#    - debug:
#        msg: "{{ sample_vars }}"
